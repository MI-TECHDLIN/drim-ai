import 'package:flutter/material.dart';

abstract class AppColors {
  static const Color anchor = Color(0xFF2E6171); // teal — primary
  static const Color sky = Color(0xFFA7CBD6); // highlights
  static const Color sage = Color(0xFF9CC5A1); // progress, growth
  static const Color apricot = Color(0xFFE9A178); // warmth, AI badge
  static const Color sand = Color(0xFFF5F1EA); // scaffold background
  static const Color surface = Color(0xFFFFFFFF); // cards, inputs
  static const Color ink = Color(0xFF1A1A1A); // primary text + borders
  static const Color muted = Color(0xFF6B7280); // secondary text
  static const Color border = Color(0xFF1A1A1A); // all neo-brutalist borders
  static const Color line = Color(0xFFE6DFD4); // dividers
  static const Color error = Color(
    0xFFC2705A,
  ); // muted terracotta — never alarm red
}
